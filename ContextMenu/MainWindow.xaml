<Window x:Class="ContextMenu.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContextMenu"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Border Height="150" Width="150" Background="RoyalBlue" CornerRadius="10">
        <TextBlock Text="Something" Foreground="Azure" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="メニュー1" Click="MenuItem_Click"/>
                <MenuItem Header="メニュー2">
                    <MenuItem Header="メニュー2-1" />
                    <MenuItem Header="メニュー2-2" />
                    <MenuItem Header="メニュー2-3" />
                </MenuItem>
                <MenuItem Header="メニュー3" />
                <MenuItem Header="メニュー4" />
            </ContextMenu>
        </Border.ContextMenu>
    </Border>
</Window>
